spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    database-platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false
    # Flyway est desactive dans le profil de test : Hibernate genere le
    # schema H2 en memoire directement a partir des entites (ddl-auto: create-drop),
    # ce qui evite toute divergence de syntaxe SQL PostgreSQL vs H2.
